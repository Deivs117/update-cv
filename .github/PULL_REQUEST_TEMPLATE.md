**Ticket:**

<!-- Obligatorio para trazabilidad. Usa "Closes #N" si este PR termina el ticket
     (lo cierra solo al mergear a main), o "Refs #N" si es un avance parcial. -->

Closes #

**Qué cambia y por qué:**

**Cómo se probó:**

<!-- npm run lint / npm run build dentro de web/, o los pasos manuales que corriste
     (por ejemplo: generar un CV de prueba, correr el pipeline de compilación LaTeX). -->
